/**
 * IUserRepository - contrato (JSDoc) para el repositorio de usuarios
 * @interface
 */

/**
 * findByEmail(email)
 * create(user)
 * findById(id)
 * approveProfessional(id)
 */

module.exports = {};
